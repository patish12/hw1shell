    hello    this 2 be
    //  line 11, line 107, cancel strcpy_s (turn it to a remark)

    //  in commands.c, define "get_command()". 

    //  in commands.c, include ctype.h for isspace()

    //  in commands.c, define "get_directory()". 

    //  in commands.c, cancel cmd_cd to remark

    //  in main.c, added printf of current directory, in order to make sure the cd command works. (IT WORKS!!!!!!!!!!!!!!!!!!!!!!!!!)
    // PAY Attention: when the input is "cd", we dont do anything with the directory.